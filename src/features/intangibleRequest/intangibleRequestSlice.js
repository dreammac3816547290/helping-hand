import { createSlice } from "@reduxjs/toolkit";

const initialState = [];

const intangibleRequestSlice = createSlice({
  name: "intangibleRequest",
  initialState,
  reducers: {},
});

export default intangibleRequestSlice.reducer;
