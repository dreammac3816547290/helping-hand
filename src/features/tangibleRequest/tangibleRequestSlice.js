import { createSlice } from "@reduxjs/toolkit";

const initialState = [];

const tangibleRequestSlice = createSlice({
  name: "tangibleRequest",
  initialState,
  reducers: {},
});

export default tangibleRequestSlice.reducer;
